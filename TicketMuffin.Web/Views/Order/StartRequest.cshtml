@model TicketMuffin.Web.Models.OrderRequestViewModel

@{
    ViewBag.Title = "StartRequest";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>There was a problem with the request:</h2>

@foreach (var error in Model.Error)
{
    <li>@error.Parameter: @error.Message</li>
}
